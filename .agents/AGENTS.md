# Workspace Instructions

- The backend server is located at `server/server.js`.
- To start the server, run `node server.js` inside the `server/` directory.
- When the user requests to "run" or "start", launch the server directly without re-planning or asking redundant setup questions.
- Frontend static assets in `public/` are served by Express at `http://localhost:4000`.
